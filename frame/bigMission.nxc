#include "sensor.nxc"
#include "move.nxc"
#include "claw.nxc"

task main(){

	goAhead(55);
	turnLeft(65);
	// Levanta garra
	moveUp(90);
	goAhead(80);
	// Abaixa garra
 	moveDown(90);
	turnRight(100);
	goAhead(110);	
	//Fazer missão da placa?!?!
	goAhead(25);
  mustStop();
}

