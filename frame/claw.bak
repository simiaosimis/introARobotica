#define POTENCY_CLAW 75

void moveUp(unsigned int angle){

     // OUT_B should be the engine with the claw
     RotateMotor(OUT_B, POTENCY, - angle);

}

void moveDown(unsigned int angle){

     RotateMotor(OUT_B, POTENCY, angle);

}

void mustStopClaw(){

	Off(OUT_B);

}


