#include "move.nxc"

task main(){

     int startingRotation = MotorRotationCount(OUT_A);
     OnFwdSync(OUT_AC, 75, -100);
     until(MotorRotationCount(OUT_A) > startingRotation + 360);
     OffEx(OUT_AC, RESET_ALL);
}
