#include "sensor.nxc"
#include "move.nxc"
#include "claw.nxc"

task main(){

	goAhead(55);
	turnLeft(64);
	// Levanta garra
	moveUp(60);
	goAhead(80);
	// Abaixa garra
 	moveDown(90);
 	OffEx(OUT_AC, RESET_ALL);
	turnRight(97);
	
	goAhead(110);	
	//Fazer missão da placa?!?!
	turnRight(10);
	goAhead(25);
   mustStop();

}

